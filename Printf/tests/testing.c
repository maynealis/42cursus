#include "../includes/libft.h"
#include <stdio.h>

int	main(void)
{
	// char	*str="Hello %s!\nand hello to you";

	// ft_putstr_fd(str, 0);
	// char *ini = ft_strchr(str, '%');
	// char *end = ft_strchr(ini, 's');
	// // printf("%ld", end - ini);
	// char c = ini[end - ini];
	// ft_putchar_fd('\n', 0);
	// ft_putchar_fd(c, 0);
	// printf("TEEEST\n");
	ft_putnbr_fd(-1234,0);
	return (0);
}